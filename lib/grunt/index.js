/*
import {rootPath} from '../index';


export {getRawFM,removeFM,readDocFile,getParsedDocFM,getParsedDocFile} from './docs';

//export * from './docs';

export function makeFontAwesomeDemo(destPath):void {
    tmp.setGracefulCleanup();
    var Jekyll, generator;
    Jekyll = require('noflo-jekyll').Jekyll;
    var dir:tmp.ISyncTMP = tmp.dirSync();
    var copyToTemp = function (copyPath:string) {
        fse.copySync(rootPath(copyPath), path.join(dir.name, copyPath));
    };
    copyToTemp('src/lib/font-awesome');
    fse['emptyDirSync'](dir.name);
    dir.removeCallback();
}

export function inspect(...args:any[]):void {
    args.forEach(function (arg) {
        process.stdout.write(util.inspect(arg, {colors: true, hidden: true, depth: 5}));
    });
}
*/
//# sourceMappingURL=index.js.map